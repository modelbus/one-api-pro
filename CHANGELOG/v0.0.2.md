# v0.0.2 — 发布脚本补丁

> 修复版本号写入错误，并更新发布脚本，确保 tag 推送流程稳定。

## 中文

### 🐛 问题修复
- 修复发布前版本号写入错误的问题，避免二进制 `common.Version` 与 tag 不一致。
- 优化自动发布脚本（`release.sh`），减少脚本执行失败概率。

---

## English

### 🐛 Bug Fixes
- Fixed the version-file write bug so the embedded `common.Version` matches the git tag.
- Polished the auto-release shell script (`release.sh`) to be more reliable during tag-driven builds.

---

## 📦 Assets / 下载

- Linux (amd64): `one-api-pro-linux-amd64`
- Linux (arm64): `one-api-pro-linux-arm64`
- Windows (amd64): `one-api-pro-windows-amd64.exe`
- macOS (amd64): `one-api-pro-darwin-amd64`
- macOS (arm64): `one-api-pro-darwin-arm64`

SHA256: see attached `checksums.txt` in the release assets.